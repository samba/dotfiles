#!/bin/bash


main () {
  local keepdays=15 # days to keep

  while getopts :k: Opt; do
    case $Opt in
      k) keepdays=$OPTARG;;
    esac
  done

  find ${HOME}/.vim/view -type f -mtime +${keepdays} -print -delete >> ${HOME}/.vim/view/deletion.log
}

main $@
